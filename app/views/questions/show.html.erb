<h1><%=@question.title%></h1>
<p><%=@question.content%></p>
<% if current_user && @question.user_id == current_user.id %>
  <%= link_to "Edit", edit_question_path(@question)%>
<%end%>

 <%=form_for :comment, html: {class: "commentform", id: "#{@question.id}"} do |f|%>
    <%=f.label :content%>
    <%=f.text_area :content%>
    <%=f.submit "Post Comment"%>
  <%end%>
<%if current_user%>
<button class= 'addComment'>Add Comment</button>
<%end%>

<ul class="comments">
<% @question.comments.each do |comment| %>
 <%= render comment %>
<%end%>
</ul>


<h2>Answers</h2>

  <%=form_for :answer, html: {class: "answerform", id: "#{@question.id}"} do |f|%>
    <%=f.label :content%>
    <%=f.text_area :content%>
    <%=f.submit "Post Answer"%>
  <%end%>
<%if current_user%>
<button class= 'addAnswer'>Add Answer</button>
<%end%>
<ul class="answers">
<% @answers.each do |answer| %>
 <%=render answer, answer: answer, layout: false%>
 <%if current_user%>
<button class= 'addAnswerComment'>Add Comment</button>
<%end%>
  <%=form_for :comment, html: {class: "answercommentform", id: "#{@question.id}"} do |f|%>
    <%=f.label :content%>
    <%=f.text_area :content%>
    <%=f.submit "Post Comment"%>
    <%end%>
<ul>
  <ul class="answercomments">
<% answer.comments.each do |comment| %>
 <%= render comment %>
<%end%>
</ul>
</ul>
<%end%>
</ul>


<% if flash[:notice]%>
  <div class="notice"><%=flash[:notice]%></div>
<%end%>
